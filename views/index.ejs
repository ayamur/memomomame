<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h1>Welcome and Merry Met, <%= user ? user.profile.name + "!" : "New Visitor!" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
  <div>
    <button type="submit" href="../views/monsters/new.ejs">🟣</button>Send Out Monster Mail!
  </div>
  <div>
    <button type="submit" href="../views/monsters/show.ejs">🟢</button>Check Monster Mailbox!
  </div>
  <div>
    <button type="submit" href="../views/monsters/new.ejs">🟠</button>User Mailboxes!
  </div>
  <div>
    <button type="submit" href="../views/monsters/new.ejs">🔵</button>Read the Tutorial!
  </div>
</main>

<%- include('./partials/footer') %>
