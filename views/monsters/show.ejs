<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
	<p>Delivered! <%= monster.owner.name %>...</p>
  <h1><%= monster.name %> 🌮</h1>
  <% if (monster.owner.equals(user?.profile._id)) { %>
    <a href="/monsters/<%= monster._id %>/edit">
      <button class="btn">Edit the Monster</button>
    </a>
    <form action="/monsters/<%= monster._id %>?_method=DELETE" method="POST">
      <button class="btn" type="submit">Delete Monster?💀</button>
    </form>
  <% } %>
</section>

<%- include('../partials/footer') %>